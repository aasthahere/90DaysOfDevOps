apiVersion: kind.x-k8s.io/v1alpha4
kind: cluster
nodes:
  - role: control-plane
    image: kindest/node:v1.35.0
  - role: worker
    image: kindest/node:v1.35.0
  - role: worker
    image: kindest/node:v1.3

  #docker login
  #kind create cluster --config kind-config.yml
  kubectl cluster -info --context
  
  
