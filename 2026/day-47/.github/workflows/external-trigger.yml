name: External Trigger

on:
  repository_dispatch:
    types: [deploy-request]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
        - name: Print environment
          run: |
           echo "Deploying to environment: ${{ github.event.client_payload.environment }}"
